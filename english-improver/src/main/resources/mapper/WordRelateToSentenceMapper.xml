<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ei.mapper.WordRelateToSentenceMapper">
    <select id="getSentenceIdByUserIdAndWordIdAndTenseId" resultType="WordRelateToSentence">
        select sentenceId from word_sentence where userId = #{userId} and wordId = #{wordId} and tenseId = #{tenseId}
    </select>
    <select id="getWordIdByUserIdAndSentenceId" resultType="WordRelateToSentence">
        select wordId from word_sentence where userId = #{userId} and sentenceId = #{sentenceId}
    </select>
</mapper>
