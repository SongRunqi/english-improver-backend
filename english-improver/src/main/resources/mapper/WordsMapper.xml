<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ei.mapper.WordsMapper">
    <select id="getWordsByUserId" resultType="Words">
        select * from words where user_id = #{userId}
    </select>
    <select id="getWordsByIdAndProperty" resultType="Words">
        select * from words where id = #{id} and property = #{property}
    </select>
    <select id="getWordsById" resultType="Words">
        select * from words where id = #{id}
    </select>
    <insert id="insertWord" parameterType="Words">
        insert into words (id, createdDate, user_id, word, pronouciation, property, means, tense)
        values (#{id}, #{created_at}, #{user_id}, #{word}, #{pronouciation}, #{property}, #{means}, #{tense})
    </insert>
</mapper>
